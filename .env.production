# 프로덕션 빌드(Vercel 등)에서는 누끼 추출 모델을 imgly CDN 에서 로드한다.
# 이렇게 하면 200MB 모델을 배포 산출물에 싣지 않아 빌드/배포가 가볍다.
# (로컬 dev 는 이 값이 적용되지 않으므로 public/models self-host 를 그대로 사용)
NEXT_PUBLIC_IMGLY_PUBLIC_PATH=https://staticimgly.com/@imgly/background-removal-data/1.4.5/dist/
